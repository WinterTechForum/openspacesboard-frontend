import { sessionListingType } from 'openspacesboard-frontend/helpers/session-listing-type';
import { module, test } from 'qunit';

module('Unit | Helper | session listing type');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = sessionListingType([42]);
  assert.ok(result);
});
